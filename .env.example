# Só a chave da API do Groq fica fora do código (é a única coisa que é um
# segredo de verdade). Os nomes e PINs de vocês os dois já estão fixos no
# server.js (CONFIG), ao estilo do Nano Bot.
GROQ_API_KEY=

# Porta do servidor (a maioria das plataformas de deploy define sozinha)
PORT=3000
